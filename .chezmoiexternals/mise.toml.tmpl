{{ if ne .chezmoi.hostname "MBA" }}
["mise"]
type = "archive"
stripComponents = 1
exact = true
executable = true
{{ $arch := .chezmoi.arch }}
{{ if eq $arch "x86_64" }}{{ $arch = "x64" }}{{ end }}
url = "https://mise.jdx.dev/mise-{{.chezmoi.os}}-{{$arch}}"
dest = ".local/bin"
{{ end }}
